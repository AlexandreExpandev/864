/**
 * @interface NumberItem
 * @summary Represents a number with its text representation, matching the backend.
 */
export interface NumberItem {
  /** The numeric value */
  number: number;
  /** The text representation */
  text: string;
}
