/**
 * @interface SelectedNumberDisplayProps
 * @summary Defines the props for the SelectedNumberDisplay component.
 */
export interface SelectedNumberDisplayProps {
  /**
   * The text representation of the selected number to display.
   */
  text: string;
}
